import { createTheme } from "@mui/material";


export const theme = createTheme({
    palette: {
        primary: {
            main: "#F4E041",
        },

        secondary: {
            main: "#00BDD3",
        },
        action: {
            disabledBackground: "#B4B4B4"
        }
    }
});